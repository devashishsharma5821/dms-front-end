import React from 'react';

const TagPopOverComponent = (props:any) => {
    // TODO, Convert all the Tag Pop Overs Into Resusable Componet. Properties / Create Experiment / Create Project ETC
    return (
        <></>
    );
};

export default TagPopOverComponent;
